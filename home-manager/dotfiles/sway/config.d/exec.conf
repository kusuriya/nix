# Startup services — order matters: dbus first, then applets
exec dbus-update-activation-environment --systemd --all
exec eww daemon
exec swaync
exec_always blueman-applet
exec_always nm-applet
exec_always kanshi
exec_always eww open bar
